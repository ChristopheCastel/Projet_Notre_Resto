{% extends 'base.html.twig' %}

{% block title %}CategorieBoisson{% endblock %}

{% block body %}
    <h1>CategorieBoisson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_boisson.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categorie_boisson.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categorie_boisson') }}">retour à la liste</a>

    <a href="{{ path('admin_categorie_boisson_edit', {'id': categorie_boisson.id}) }}">Modifier</a>

    {{ include('admin_categorie_boisson/_delete_form.html.twig') }}
{% endblock %}
