{% extends 'base.html.twig' %}

{% block title %}Boisson{% endblock %}

{% block body %}
    <h1>Boisson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boisson.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ boisson.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ boisson.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ boisson.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_boisson_index') }}">retour à la liste</a>

    <a href="{{ path('admin_boisson_edit', {'id': boisson.id}) }}">modifier</a>

    {{ include('admin_boisson/_delete_form.html.twig') }}
{% endblock %}
