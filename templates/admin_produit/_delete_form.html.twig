<form method="post" action="{{ path('app_admin_produit_delete', {'id': produit.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce produit ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
     <button class="btn"><img class="icone bg-white" src="{{ asset('images/delete.jpg')}}" alt=""> </button>
</form>
